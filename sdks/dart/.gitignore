# The uniffi-bindgen-dart output (lib/src/rust/frf.dart) IS committed — see GENERATED.md.
# Only the compiled native library is build output, not source:
lib/src/rust/*.dylib
lib/src/rust/*.so
lib/src/rust/*.dll

# Dart package cache
.dart_tool/
.pub-cache/
pubspec.lock

# Build outputs
build/

# Flutter platform directories created if scaffold is run later
ios/
android/
linux/
macos/
windows/
web/
