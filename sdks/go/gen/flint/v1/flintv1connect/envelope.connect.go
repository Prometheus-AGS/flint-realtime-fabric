// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: flint/v1/envelope.proto

package flintv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/prometheusags/frf/sdks/go/gen/flint/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SpineServiceName is the fully-qualified name of the SpineService service.
	SpineServiceName = "flint.v1.SpineService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SpineServicePublishProcedure is the fully-qualified name of the SpineService's Publish RPC.
	SpineServicePublishProcedure = "/flint.v1.SpineService/Publish"
	// SpineServiceSubscribeProcedure is the fully-qualified name of the SpineService's Subscribe RPC.
	SpineServiceSubscribeProcedure = "/flint.v1.SpineService/Subscribe"
	// SpineServiceAckProcedure is the fully-qualified name of the SpineService's Ack RPC.
	SpineServiceAckProcedure = "/flint.v1.SpineService/Ack"
)

// SpineServiceClient is a client for the flint.v1.SpineService service.
type SpineServiceClient interface {
	Publish(context.Context, *connect.Request[v1.PublishRequest]) (*connect.Response[v1.PublishResponse], error)
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.EventEnvelope], error)
	Ack(context.Context, *connect.Request[v1.AckRequest]) (*connect.Response[v1.AckResponse], error)
}

// NewSpineServiceClient constructs a client for the flint.v1.SpineService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSpineServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SpineServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	spineServiceMethods := v1.File_flint_v1_envelope_proto.Services().ByName("SpineService").Methods()
	return &spineServiceClient{
		publish: connect.NewClient[v1.PublishRequest, v1.PublishResponse](
			httpClient,
			baseURL+SpineServicePublishProcedure,
			connect.WithSchema(spineServiceMethods.ByName("Publish")),
			connect.WithClientOptions(opts...),
		),
		subscribe: connect.NewClient[v1.SubscribeRequest, v1.EventEnvelope](
			httpClient,
			baseURL+SpineServiceSubscribeProcedure,
			connect.WithSchema(spineServiceMethods.ByName("Subscribe")),
			connect.WithClientOptions(opts...),
		),
		ack: connect.NewClient[v1.AckRequest, v1.AckResponse](
			httpClient,
			baseURL+SpineServiceAckProcedure,
			connect.WithSchema(spineServiceMethods.ByName("Ack")),
			connect.WithClientOptions(opts...),
		),
	}
}

// spineServiceClient implements SpineServiceClient.
type spineServiceClient struct {
	publish   *connect.Client[v1.PublishRequest, v1.PublishResponse]
	subscribe *connect.Client[v1.SubscribeRequest, v1.EventEnvelope]
	ack       *connect.Client[v1.AckRequest, v1.AckResponse]
}

// Publish calls flint.v1.SpineService.Publish.
func (c *spineServiceClient) Publish(ctx context.Context, req *connect.Request[v1.PublishRequest]) (*connect.Response[v1.PublishResponse], error) {
	return c.publish.CallUnary(ctx, req)
}

// Subscribe calls flint.v1.SpineService.Subscribe.
func (c *spineServiceClient) Subscribe(ctx context.Context, req *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.EventEnvelope], error) {
	return c.subscribe.CallServerStream(ctx, req)
}

// Ack calls flint.v1.SpineService.Ack.
func (c *spineServiceClient) Ack(ctx context.Context, req *connect.Request[v1.AckRequest]) (*connect.Response[v1.AckResponse], error) {
	return c.ack.CallUnary(ctx, req)
}

// SpineServiceHandler is an implementation of the flint.v1.SpineService service.
type SpineServiceHandler interface {
	Publish(context.Context, *connect.Request[v1.PublishRequest]) (*connect.Response[v1.PublishResponse], error)
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.EventEnvelope]) error
	Ack(context.Context, *connect.Request[v1.AckRequest]) (*connect.Response[v1.AckResponse], error)
}

// NewSpineServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSpineServiceHandler(svc SpineServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	spineServiceMethods := v1.File_flint_v1_envelope_proto.Services().ByName("SpineService").Methods()
	spineServicePublishHandler := connect.NewUnaryHandler(
		SpineServicePublishProcedure,
		svc.Publish,
		connect.WithSchema(spineServiceMethods.ByName("Publish")),
		connect.WithHandlerOptions(opts...),
	)
	spineServiceSubscribeHandler := connect.NewServerStreamHandler(
		SpineServiceSubscribeProcedure,
		svc.Subscribe,
		connect.WithSchema(spineServiceMethods.ByName("Subscribe")),
		connect.WithHandlerOptions(opts...),
	)
	spineServiceAckHandler := connect.NewUnaryHandler(
		SpineServiceAckProcedure,
		svc.Ack,
		connect.WithSchema(spineServiceMethods.ByName("Ack")),
		connect.WithHandlerOptions(opts...),
	)
	return "/flint.v1.SpineService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SpineServicePublishProcedure:
			spineServicePublishHandler.ServeHTTP(w, r)
		case SpineServiceSubscribeProcedure:
			spineServiceSubscribeHandler.ServeHTTP(w, r)
		case SpineServiceAckProcedure:
			spineServiceAckHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSpineServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSpineServiceHandler struct{}

func (UnimplementedSpineServiceHandler) Publish(context.Context, *connect.Request[v1.PublishRequest]) (*connect.Response[v1.PublishResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("flint.v1.SpineService.Publish is not implemented"))
}

func (UnimplementedSpineServiceHandler) Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.EventEnvelope]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("flint.v1.SpineService.Subscribe is not implemented"))
}

func (UnimplementedSpineServiceHandler) Ack(context.Context, *connect.Request[v1.AckRequest]) (*connect.Response[v1.AckResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("flint.v1.SpineService.Ack is not implemented"))
}
