// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: flint/v1/signal.proto

package flintv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/prometheusags/frf/sdks/go/gen/flint/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SignalServiceName is the fully-qualified name of the SignalService service.
	SignalServiceName = "flint.v1.SignalService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SignalServiceSignalProcedure is the fully-qualified name of the SignalService's Signal RPC.
	SignalServiceSignalProcedure = "/flint.v1.SignalService/Signal"
)

// SignalServiceClient is a client for the flint.v1.SignalService service.
type SignalServiceClient interface {
	Signal(context.Context) *connect.BidiStreamForClient[v1.SignalEnvelope, v1.SignalEnvelope]
}

// NewSignalServiceClient constructs a client for the flint.v1.SignalService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSignalServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SignalServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	signalServiceMethods := v1.File_flint_v1_signal_proto.Services().ByName("SignalService").Methods()
	return &signalServiceClient{
		signal: connect.NewClient[v1.SignalEnvelope, v1.SignalEnvelope](
			httpClient,
			baseURL+SignalServiceSignalProcedure,
			connect.WithSchema(signalServiceMethods.ByName("Signal")),
			connect.WithClientOptions(opts...),
		),
	}
}

// signalServiceClient implements SignalServiceClient.
type signalServiceClient struct {
	signal *connect.Client[v1.SignalEnvelope, v1.SignalEnvelope]
}

// Signal calls flint.v1.SignalService.Signal.
func (c *signalServiceClient) Signal(ctx context.Context) *connect.BidiStreamForClient[v1.SignalEnvelope, v1.SignalEnvelope] {
	return c.signal.CallBidiStream(ctx)
}

// SignalServiceHandler is an implementation of the flint.v1.SignalService service.
type SignalServiceHandler interface {
	Signal(context.Context, *connect.BidiStream[v1.SignalEnvelope, v1.SignalEnvelope]) error
}

// NewSignalServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSignalServiceHandler(svc SignalServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	signalServiceMethods := v1.File_flint_v1_signal_proto.Services().ByName("SignalService").Methods()
	signalServiceSignalHandler := connect.NewBidiStreamHandler(
		SignalServiceSignalProcedure,
		svc.Signal,
		connect.WithSchema(signalServiceMethods.ByName("Signal")),
		connect.WithHandlerOptions(opts...),
	)
	return "/flint.v1.SignalService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SignalServiceSignalProcedure:
			signalServiceSignalHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSignalServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSignalServiceHandler struct{}

func (UnimplementedSignalServiceHandler) Signal(context.Context, *connect.BidiStream[v1.SignalEnvelope, v1.SignalEnvelope]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("flint.v1.SignalService.Signal is not implemented"))
}
