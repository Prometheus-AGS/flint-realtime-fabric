POSITION REMINDER — read this as your FIRST tool call every turn
Phase: issue-triage-and-architecture-audit
Step: 5 of 6
Stage: execute_ready
Next command: /kbd-apply p38-c006-dead-code-and-guard-classification

Required signal format (emit BEFORE any tool call):
  Starting <kbd-skill> — issue-triage-and-architecture-audit (step 5 of 6)
  [do work]
  Completed <kbd-skill> — issue-triage-and-architecture-audit (step 5 of 6)

N/T source: progress.json -> completion.implementation {completed: 5, total: 6}.

Change status:
  p38-c001-iggy-handshake-diagnosis  DONE + PROVEN, archived (3437b95)
  p38-c002-prove-channel-id-fix      DONE + PROVEN, archived (851b328) — guard
                                     observed to FAIL under sabotage. ISSUE #2 CLOSED.
  p38-c003-e2e-tenant-uuid           DONE, NOT PROVEN, archived (3af3e93) — static
                                     proof only; issue #7 left OPEN
  p38-c004-gateway-main-size...      DONE + PROVEN, archived (139d3c7)
  p38-c005-claude-md-adr-recon...    DONE, archived — docs reconciled to the ADRs
  p38-c006-dead-code-and-guard...    LAST ONE. ws.rs deletion needs nothing; the
                                     #[ignore]d-test classification wants a live
                                     server (Docker is UP).

*** FOREIGN UNCOMMITTED WORK IN THE TREE — DO NOT COMMIT IT BLIND ***
  crates/frf-app/src/shape/mod.rs
  crates/frf-app/src/shape/tests.rs
  crates/frf-gateway/src/routes/shape.rs
All three carry mtime 11:57:55, predating this session's c005 work. The diff is
deliberate ADR-009 shape-lease engineering: a new ShapeUseCaseError::HandleExpired
variant, a same_authority comparison distinguishing an expired lease from a genuine
handle mismatch, and a matching test. It is NOT mine. c005 excluded it by scoping
git add to an explicit pathspec, verified with `git add --dry-run` before staging.
c006 must do the same — it touches crates/, so blanket `git add -A crates/` WOULD
sweep this in.

GITHUB ISSUES: #2 CLOSED (proven). #6 CLOSED (IPv6 root cause). #7 OPEN (c003
statically proven, never observed publishing). #8 OPEN (46 archived changes with
unticked tasks).

OPEN FINDINGS recorded, not fixed:
  1. ci.yml:50-61 runs `cargo test --all` — violates AGENTS.md's "CI/CD is never
     used to run tests. Ever." Predates the policy (6e549e8).
  2. ci.yml:45 is RED: clippy --features dev-endpoints fails E0063 "missing field
     `subject`" at routes/dev.rs:161. Proven pre-existing by stashing vs HEAD.
  3. dagger/codegen.ts stage 4 (frb-dart) is doubly unsatisfiable — flagged in
     c005, not fixed.

KNOWN DEFECT (eighth occurrence): this file goes stale at every transition; nothing
in end-task or archive rewrites it. Authoritative: current-waypoint.json +
progress.json. Carry to /kbd-reflect as a process defect.
