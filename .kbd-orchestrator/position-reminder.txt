POSITION REMINDER — read this as your FIRST tool call every turn
Phase: issue-triage-and-architecture-audit
Step: 4 of 6
Stage: execute_ready
Next command: /kbd-apply p38-c005-claude-md-adr-reconciliation

Required signal format (emit BEFORE any tool call):
  Starting <kbd-skill> — issue-triage-and-architecture-audit (step 4 of 6)
  [do work]
  Completed <kbd-skill> — issue-triage-and-architecture-audit (step 4 of 6)

N/T source: progress.json -> completion.implementation {completed: 4, total: 6}.
Never estimate; never use evidence-task completion as the counter.

Change status:
  p38-c001-iggy-handshake-diagnosis  DONE + PROVEN, archived (commit 3437b95)
  p38-c002-prove-channel-id-fix      DONE + PROVEN, archived — guard observed to FAIL
                                     under sabotage with the verbatim issue #2 error.
                                     ISSUE #2 CLOSED on demonstrated receipt.
  p38-c003-e2e-tenant-uuid           DONE, NOT PROVEN, archived (commit 3af3e93) —
                                     static proof only; issue #7 left OPEN
  p38-c004-gateway-main-size...      DONE + PROVEN, archived (commit 139d3c7) —
                                     main.rs 504 -> 425; size check sabotage-tested
  p38-c005-claude-md-adr-recon...    pending — docs only, no infrastructure needed
  p38-c006-dead-code-and-guard...    pending — ws.rs deletion needs nothing; the
                                     #[ignore]d-test classification wants a live
                                     server (Docker is UP, so available on demand)

GITHUB ISSUES: #2 CLOSED (proven). #6 CLOSED (IPv6 root cause). #7 OPEN — c003's fix is
statically proven but no live publish was ever observed. #8 OPEN — 46 archived changes
have unticked tasks; that lint is wired to nothing.

ENVIRONMENT: Docker/OrbStack UP. No containers of mine are running — c002's server was
torn down after use. aso-prior-auth-iggy-server-1 belongs to another project; leave it.

OPEN FINDINGS from c004, recorded but NOT fixed (out of scope there):
  1. ci.yml:50-61 runs `cargo test --all`, violating AGENTS.md's non-negotiable
     "CI/CD is never used to run tests. Ever." Pre-dates the policy (traced to 6e549e8).
  2. ci.yml:45 is RED today: `clippy --features dev-endpoints` fails with E0063
     "missing field `subject`" at routes/dev.rs:161. Proven pre-existing by stashing
     and re-running against HEAD.

KNOWN DEFECT (seventh occurrence): this file goes stale at every transition — nothing in
end-task or archive rewrites it. Authoritative sources are current-waypoint.json and
progress.json. Carry to /kbd-reflect as a process defect, not another one-off patch.
