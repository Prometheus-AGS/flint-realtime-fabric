# Tasks — p14-c001-oathkeeper-jwks

- [ ] Create `deploy/oathkeeper/jwks.json` with dev HMAC JWKS
- [ ] Verify JSON structure is valid (kty, use, kid, k, alg fields present)
- [ ] Confirm `config.yml` jwks_urls path matches the mounted file path
