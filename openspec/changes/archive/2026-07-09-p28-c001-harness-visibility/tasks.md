# Tasks — p28-c001-harness-visibility

- [x] 1. media-decode.spec.ts: drop the connectToSovereignSfu pre-check; add test.setTimeout(60_000). Typecheck.
- [x] 2. run-media-decode.sh: dump gateway logs before teardown on failure (capture to a file, not just stderr). shellcheck. QA gate.
