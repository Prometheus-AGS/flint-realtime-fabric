# Tasks — p17-c002

- [x] In frf-gateway config validate(): error when JWT_ISSUER unset and not dev-endpoints build
- [x] Keep the dev-build warning path; add a test for the prod-missing-issuer error
- [x] Document JWT_ISSUER as required-in-prod in docs/ENVIRONMENT.md
