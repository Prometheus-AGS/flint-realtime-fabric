# Tasks — p18-c008

- [x] Implement PDS session/auth (com.atproto.server.createSession or app-password) for the configured identity
- [x] send(): map FederatedEvent → a record and POST com.atproto.repo.createRecord
- [x] Replace the Err(unimplemented) stub; handle auth-refresh + errors
- [x] Test the write-path mapping with a mocked PDS; clippy/unwrap clean
