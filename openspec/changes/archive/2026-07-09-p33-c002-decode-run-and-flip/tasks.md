# Tasks — p33-c002-decode-run-and-flip

- [x] 1. Re-run HOST_NET=1 run-media-decode.sh; record the ACTUAL outcome + diagnostics in docs/PHASE-33-DECODE-RESULT.md.
- [x] 2. If framesDecoded>0: flip main.rs sovereign branch + SECURITY §6 + CHANGELOG + PHASE-33-SIGNOFF. Else: re-affirm gated (+ TURN fallback). Re-run the release gate; QA gate.
