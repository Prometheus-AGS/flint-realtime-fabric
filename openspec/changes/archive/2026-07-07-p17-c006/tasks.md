# Tasks — p17-c006

- [x] frf-sdk-rust: add Sync/Agent/Signal client wrappers (parity with TS/Go) + Entity/Authz clients (new servers)
- [x] frf-ffi: route subscribe through resilient_subscribe (reconnect/replay), expose ack over FFI
- [x] Update TS/Go/C# thin wrappers to include Entity/Authz now that servers exist
- [x] Tests for the new client methods; clippy/unwrap clean
