# Tasks — p27-c003-room-join-fanout

- [x] 1. Harness: after the offer, both sender + receiver send a `RoomJoin` frame for the shared room so RoomRouter co-locates them and fans out the sender's RTP. Typecheck. QA gate.
