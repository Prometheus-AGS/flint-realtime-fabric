# p9-c005 — UniFFI Binding Generation + `sdks/` Scaffold

## Summary

Add `crates/uniffi-bindgen/` workspace binary; generate Swift + Kotlin bindings
from `frf-ffi`; commit generated files; add minimal `Package.swift` and `build.gradle.kts`.

## Files to Create

- `crates/uniffi-bindgen/Cargo.toml` — `[[bin]] uniffi-bindgen` with `uniffi` cli feature
- `crates/uniffi-bindgen/src/main.rs` — `fn main() { uniffi::uniffi_bindgen_main() }`
- `sdks/swift/Sources/FrfFfi/frf.swift` — generated by uniffi-bindgen
- `sdks/swift/Sources/FrfFfi/frf.modulemap` — generated
- `sdks/swift/Package.swift` — minimal SPM package scaffold
- `sdks/kotlin/lib/src/main/kotlin/uniffi/frf/frf.kt` — generated
- `sdks/kotlin/build.gradle.kts` — minimal Gradle scaffold

## Files to Modify

- `Cargo.toml` — add `"crates/uniffi-bindgen"` to workspace members
- `dagger/codegen.ts` — Stages 2–3 use `cargo run --bin uniffi-bindgen` (already wired; confirm path)

## Exit Criteria

- `cargo build --bin uniffi-bindgen` succeeds
- `sdks/swift/Sources/FrfFfi/frf.swift` exists and is committed
- `cargo check --workspace` passes
