# Tasks — p25-c002-live-decode-run

- [x] 1. Execute `scripts/run-media-decode.sh` (honest live run) and record the ACTUAL outcome in `docs/PHASE-25-DECODE-RESULT.md`: framesDecoded>0 observed → note it (c003 flips); else the concrete blocker + a media-path-vs-harness diagnosis (c003 re-affirms).
