# Tasks — p32-c002-decode-run-and-flip

- [x] 1. Re-run run-media-decode.sh (HTTPS via caddy); record the ACTUAL outcome + diagnostics in docs/PHASE-32-DECODE-RESULT.md.
- [x] 2. If framesDecoded>0: flip main.rs sovereign branch + SECURITY §6 + CHANGELOG + PHASE-32-SIGNOFF. Else: re-affirm gated (+ env-pivot if warranted). Re-run the release gate; QA gate.
